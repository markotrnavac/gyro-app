# Copy to .env for local development.
# Railway sets DATABASE_URL automatically when you add a PostgreSQL plugin.
# For local dev, install PostgreSQL and create a database, then fill in:
DATABASE_URL=postgresql://user:password@localhost:5432/gyroapp
